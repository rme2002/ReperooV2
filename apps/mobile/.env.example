# Environment Variables Example
# Copy this to .env.local for local development

# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# API Configuration
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1

# Note:
# - .env.dev is used for development builds (eas build --profile dev)
# - .env.prd is used for production builds (eas build --profile prd)
# - .env.local is for local development only (not committed to git)
# - All variables must be prefixed with EXPO_PUBLIC_ to be accessible in the app
