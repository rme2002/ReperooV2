# Mobile App

Expo / React Native client for the Pulse workspace. This project is part of the monorepo but runs locally outside of Docker for faster iteration.

## Requirements

- Node.js 20+
- Xcode (for iOS simulator) or Android Studio (for Android)
- Expo CLI (installed automatically via `npx`)

## Getting Started

```bash
npm install
npx expo run:ios
```
